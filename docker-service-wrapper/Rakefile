require_relative '../rake/helper'

image "service-wrapper" do
  @tomcat = ENV["tomcat"] || "tomcat8"
  @container_build_args = "#{@container_build_args} --build-arg TOMCAT=#{@tomcat}"
  if @tomcat != "tomcat8"
    @name += "-" + @tomcat
  end
end
