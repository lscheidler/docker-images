vault_version: 1.6.3
vault_source_sha256: 844adaf632391be41f945143de7dccfa9b39c52a72e8e22a5d6bad9c32404c46

resources:
  - name: vault
    url: "https://releases.hashicorp.com/vault/{{ vault_version }}/vault_{{ vault_version }}_linux_amd64.zip"
    destination_file: vault.zip
    checksum_type: sha256
    checksum: "{{ vault_source_sha256 }}"
    unarchive: true
